// Placeholder — WP-C implements the fixed 720px white JPEG-export sheet
// per the plan's "JPEG export" spec: brand-800 top bar + gold hairline,
// property Thai name + สรุปยอดรายรับ–รายจ่ายประจำวัน + BE date, income
// table (all active rows, blank = "-"), double-rule รวมรายรับ, expense
// section (omitted when empty) + รวมรายจ่าย, tinted cash-note box with
// bold burgundy deposit total, muted footer. Rendered VISIBLE (never
// offscreen) so html2canvas-pro captures it faithfully. Must render
// ../fixtures.ts's fixtureDaySheet standalone for WP-C's own acceptance
// check (self-inspect the exported JPEG, incl. stacked Thai vowels/tones).

export function ReportSheet() {
  return null;
}
