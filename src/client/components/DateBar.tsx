// Placeholder — WP-B implements the date-navigation bar (prev/next arrows +
// native date input, Buddhist Era display, RDR's multi-digit date-input
// fix) used on /:property/day/:date and /:property/report/:date.

export function DateBar() {
  return null;
}
