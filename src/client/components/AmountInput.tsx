// Placeholder — WP-B implements the money input used by income cells and
// expense-line amounts: inputmode="decimal", right-aligned tabular-nums,
// save-on-blur via src/shared/money.ts's parseAmountToSatang/formatSatang.

export function AmountInput() {
  return null;
}
